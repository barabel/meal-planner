# Notes

## В работе
<!-- Что реализую сейчас. Задачи, заметки, решения по текущей фиче. -->

### Редактирование рецепта
- [ ] IPC-канал `recipes:update` (ipc-channels + controller + preload + .d.ts)
- [ ] `IUpdateRecipeDto` в domain/recipe
- [ ] `RecipeService.update()` + `IRecipeRepository.update()`
- [ ] `RecipeStoreRepository.update()` — найти по id, заменить
- [ ] `useRecipesStore.updateRecipe()`
- [ ] UI: форма редактирования (переиспользовать create-recipe или отдельный виджет?)

## Бэклог
<!-- Что нужно сделать потом. Конкретные задачи, без срока. -->

- [ ] Валидация входящих DTO на бэке (в `RecipeService` или отдельный слой) — сейчас пустой title сохраняется без ошибки

## Идеи
<!-- Фичи и расширения, которые хочу добавить. Не обязательно реализовывать. -->

## Интересности
<!-- Паттерны, либы, концепции — наткнулся, хочу изучить или попробовать. -->
